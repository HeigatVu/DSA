/*
Input: 
Output:
Constraints:
*/

#include <bits/stdc++.h>
#define fastio                                                                 \
  ios_base::sync_with_stdio(0);                                                \
  cin.tie(0);                                                                  \
  cout.tie(0)

int maxMoney(int numDash, int numUnderline) {
  if (numDash < 2 || (numDash + numUnderline) < 3) {
    return 0;
  }
  int maxMoney = 0;
  
}
